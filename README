#Robotics Control Lab 4.1
Este proyecto implementa y evalúa un controlador **PID por articulación** para el brazo robótico SO-101 utilizando **MuJoCo** como simulador principal y **Gazebo** para validación mediante reproducción de trayectorias.

El objetivo del estudio es analizar el comportamiento del sistema bajo distintas configuraciones de ganancias (P, PI, PD y PID), evaluando velocidad de respuesta, overshoot, oscilaciones, error estacionario y esfuerzo de control.

---

## 📁 Estructura del Proyecto
simulation_code/
├─ model/
│ ├─ scene_urdf.xml
│ ├─ robot_from_urdf.xml
│ └─ assets/
├─ data/
│ └─ runs/
├─ run_mujoco_simulation.py
├─ run_all_experiments.py
├─ plot_csv.py
├─ export_reference_from_csv.py
├─ gazebo_replay.py
├─ so101_control.py
├─ so101_mujoco_pid_utils.py
├─ so101_mujoco_utils2.py
└─ mujoco_simulation.py
---

## ⚙️ Requisitos

- Ubuntu / Linux
- Python 3.10+
- MuJoCo
- numpy
- pandas
- matplotlib
- dash y plotly (opcional para visualización en tiempo real)

---

## 🔧 Instalación del Entorno

Desde la carpeta del proyecto:

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -U pip
pip install mujoco numpy pandas matplotlib dash plotly
python -c "import mujoco; print(mujoco.__version__)"
source .venv/bin/activate
python run_mujoco_simulation.py --family PID --combo_id test
export MUJOCO_GL=osmesa
python run_mujoco_simulation.py --headless --no_plot --family PID --combo_id test
export MUJOCO_GL=osmesa
python run_all_experiments.py --outdir data/runs --headless --no-plot \
  --duration-move 2.0 --duration-hold 2.0
Se generarán:

CSV individuales por configuración

Gráficas PNG automáticas

all_results.csv con todos los resultados combinados
python plot_csv.py data/runs/PID_1.csv
Las gráficas incluyen:

Posición articular vs tiempo

Velocidad articular

Torque total aplicado
Se pueden activar perfiles de disturbio:

off

smooth

realistic

stress

Ejemplo:

python run_mujoco_simulation.py --disturb_profile smooth --disturb_scale 1.0
